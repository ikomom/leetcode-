给定一组整数数组，和一个target值，这个值由数组内的两个数相加而成，返回这两数的位置组成的数组。
【每个输入都只有精确的一个解：也就是输入的数组的数字不能够重复吧？】
【不能使用一个元素两次】
Given nums = [2, 7, 11, 15], target = 9,
Because nums[0] + nums[1] = 2 + 7 = 9,
return [0, 1].


我的思路是，首先注意返回值是一个数组，那么可以创建一个空的数组装下标值。接着，遍历数组，找出是否有相加值等于target的，有的话返回下标值
这个思路嵌套循环，不太好，耗费时间长，无用操作多，当测试用例复杂时就不起作用了（比如传进去的数组有两对可以加上的target）。



